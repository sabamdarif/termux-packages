diff --git a/panels/applications/cc-snap-row.c b/panels/applications/cc-snap-row.c
index f06318d..c78115e 100644
--- a/panels/applications/cc-snap-row.c
+++ b/panels/applications/cc-snap-row.c
@@ -351,8 +351,6 @@ make_interface_label (const gchar *interface_name)
     return _("Change time server settings");
   else if (strcmp (interface_name, "timezone-control") == 0)
     return _("Change the time zone");
-  else if (strcmp (interface_name, "udisks2") == 0)
-    return _("Access the UDisks2 service for configuring disks and removable media");
   else if (strcmp (interface_name, "upower-observe") == 0)
     return _("Access energy usage data");
   else if (strcmp (interface_name, "u2f-devices") == 0)
diff --git a/panels/system/about/cc-system-details-window.c b/panels/system/about/cc-system-details-window.c
index 028ed1d..23c2d04 100644
--- a/panels/system/about/cc-system-details-window.c
+++ b/panels/system/about/cc-system-details-window.c
@@ -33,9 +33,6 @@
 #include <glibtop/mountlist.h>
 #include <glibtop/mem.h>
 #include <glibtop/sysinfo.h>
-#include <udisks/udisks.h>
-#include <gudev/gudev.h>
-
 #include <gdk/gdk.h>
 
 #ifdef GDK_WINDOWING_WAYLAND
@@ -349,53 +346,85 @@ get_os_type (void)
 {
   if (GLIB_SIZEOF_VOID_P == 8)
     /* translators: This is the type of architecture for the OS */
-    return g_strdup_printf (_("64-bit"));
+    return g_strdup_printf ("%s", _("64-bit"));
   else
     /* translators: This is the type of architecture for the OS */
-    return g_strdup_printf (_("32-bit"));
+    return g_strdup_printf ("%s", _("32-bit"));
 }
 
 char *
 get_primary_disk_info (void)
 {
-  g_autoptr(UDisksClient) client = NULL;
-  GDBusObjectManager *manager;
-  g_autolist(GDBusObject) objects = NULL;
-  GList *l;
-  guint64 total_size;
   g_autoptr(GError) error = NULL;
-
-  total_size = 0;
-
-  client = udisks_client_new_sync (NULL, &error);
-  if (client == NULL)
+  g_autofree char *contents = NULL;
+  g_auto(GStrv) lines = NULL;
+  guint64 total_size = 0;
+  
+  /* Read /proc/partitions to get disk information */
+  if (!g_file_get_contents ("/proc/partitions", &contents, NULL, &error))
     {
-      g_warning ("Unable to get UDisks client: %s. Disk information will not be available.",
+      g_warning ("Unable to read /proc/partitions: %s. Disk information will not be available.",
                  error->message);
       return NULL;
     }
 
-  manager = udisks_client_get_object_manager (client);
-  objects = g_dbus_object_manager_get_objects (manager);
-
-  for (l = objects; l != NULL; l = l->next)
+  lines = g_strsplit (contents, "\n", -1);
+  
+  for (int i = 0; lines[i] != NULL; i++)
     {
-      UDisksDrive *drive;
-      drive = udisks_object_peek_drive (UDISKS_OBJECT (l->data));
-
-      /* Skip removable devices */
-      if (drive == NULL ||
-          udisks_drive_get_removable (drive) ||
-          udisks_drive_get_ejectable (drive))
+      g_auto(GStrv) fields = NULL;
+      const char *device_name;
+      guint64 blocks;
+      
+      /* Skip header and empty lines */
+      if (g_str_has_prefix (lines[i], "major") || 
+          g_strstrip (lines[i])[0] == '\0')
+        continue;
+        
+      fields = g_strsplit_set (g_strstrip (lines[i]), " \t", -1);
+      
+      /* Need at least 4 fields: major minor blocks name */
+      if (g_strv_length (fields) < 4)
+        continue;
+        
+      /* Skip fields that are empty due to multiple spaces/tabs */
+      int field_count = 0;
+      char *clean_fields[4] = {NULL};
+      for (int j = 0; fields[j] != NULL && field_count < 4; j++)
         {
-          continue;
+          if (strlen (g_strstrip (fields[j])) > 0)
+            clean_fields[field_count++] = fields[j];
+        }
+        
+      if (field_count < 4)
+        continue;
+        
+      device_name = clean_fields[3];
+      
+      /* Skip partitions (contain numbers) and loop devices */
+      if (strpbrk (device_name, "0123456789") != NULL ||
+          g_str_has_prefix (device_name, "loop") ||
+          g_str_has_prefix (device_name, "ram"))
+        continue;
+        
+      /* Only count main disk devices (sda, sdb, nvme0n1, etc.) */
+      if (!g_str_has_prefix (device_name, "sd") &&
+          !g_str_has_prefix (device_name, "hd") &&
+          !g_str_has_prefix (device_name, "nvme") &&
+          !g_str_has_prefix (device_name, "mmcblk") &&
+          !g_str_has_prefix (device_name, "vd"))
+        continue;
+        
+      blocks = g_ascii_strtoull (clean_fields[2], NULL, 10);
+      if (blocks > 0)
+        {
+          /* blocks are in 1024-byte units */
+          total_size += blocks * 1024;
         }
-
-      total_size += udisks_drive_get_size (drive);
     }
 
   if (total_size > 0)
-      return g_format_size (total_size);
+    return g_format_size (total_size);
 
   return NULL;
 }
@@ -623,6 +652,7 @@ get_ram_size_libgtop (void)
   return mem.total;
 }
 
+#if defined(HAVE_GUDEV)
 guint64
 get_ram_size_dmi (void)
 {
@@ -646,9 +676,18 @@ get_ram_size_dmi (void)
   }
   return ram_total;
 }
+#else
+guint64
+get_ram_size_dmi (void)
+{
+  return 0;
+}
+#endif
 
 static void
-system_details_window_title_print_padding (const gchar *title, GString *dst_string, gsize maxlen)
+system_details_window_title_print_padding (const gchar *title, 
+                                          GString *dst_string, 
+                                          gsize maxlen)
 {
   gsize title_len;
   gsize maxpad = maxlen;
@@ -658,9 +697,10 @@ system_details_window_title_print_padding (const gchar *title, GString *dst_stri
 
   if (title == NULL || dst_string == NULL)
     return;
-  g_string_append_printf (dst_string, "%s", title);
 
+  g_string_append_printf (dst_string, "%s", title);
   title_len = g_utf8_strlen (title, -1) + 1;
+  
   for (gsize i = title_len; i < maxpad; i++)
     g_string_append (dst_string, " ");
 }
diff --git a/panels/system/meson.build b/panels/system/meson.build
index a9ac171..8ed6265 100644
--- a/panels/system/meson.build
+++ b/panels/system/meson.build
@@ -80,7 +80,6 @@ sources += gnome.mkenums_simple(
 libsecret_dep = dependency('libsecret-1')
 gnutls_dep = dependency('gnutls')
 libgtop2 = dependency('libgtop-2.0')
-udisks2 = dependency('udisks2', version: '>= 2.8.2')
 
 # Kerberos support
 krb_dep = dependency('krb5', required: false)
@@ -92,14 +91,12 @@ deps = common_deps + [
   gdk_pixbuf_dep,
   gnome_desktop_dep,
   gnutls_dep,
-  gudev_dep,
   libgtop2,
   liblanguage_dep,
   libsecret_dep,
   krb_dep,
   polkit_gobject_dep,
   pwquality_dep,
-  udisks2,
 ]
 
 if enable_ibus
diff --git a/panels/system/region/cc-format-preview.c b/panels/system/region/cc-format-preview.c
index 895f063..547b3af 100644
--- a/panels/system/region/cc-format-preview.c
+++ b/panels/system/region/cc-format-preview.c
@@ -133,7 +133,11 @@ update_format_examples (CcFormatPreview *self)
   else
     old_locale = uselocale (locale);
 
-  fmt = nl_langinfo (_NL_MEASUREMENT_MEASUREMENT);
+  #ifdef _NL_MEASUREMENT_MEASUREMENT
+    fmt = nl_langinfo (_NL_MEASUREMENT_MEASUREMENT);
+  #else
+    fmt = "1"; /* Default metric for musl */
+  #endif
   /* The returned pointer of nl_langinfo could be invalid after switching
      locale, so we must use it here. */
   is_imperial = fmt && *fmt == 2;
